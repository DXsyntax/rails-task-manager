<%= form_for(@task) do |task| %>
  <%= task.label :title %>
  <%= task.text_field :title %>

  <%= task.label :details %>
  <%= task.text_field :details %>

  <%= task.submit %>
<% end %>


<%= render 'form', tasks: @restaurant %> <!-- no fucking idea what this does for me -->
